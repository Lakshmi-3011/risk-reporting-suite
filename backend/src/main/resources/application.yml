spring:
  application:
    name: risk-reporting-suite

  datasource:
    url: jdbc:postgresql://localhost:5432/risk_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

# Day 2 Completed